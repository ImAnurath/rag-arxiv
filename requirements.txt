# Phase 1
arxiv==2.1.0 # For downloading papers from arXiv
pymupdf==1.24.5 # For extracting text from PDFs
langchain-text-splitters==0.2.2 # For splitting text into chunks
pydantic==2.7.1 # For data validation and settings management
pydantic-settings==2.3.4 # For managing application settings
python-dotenv==1.0.1 # For loading environment variables from .env files
loguru==0.7.2 # For logging
httpx==0.27.0 # For making HTTP requests

# Phase 2
sentence-transformers==3.0.1 # For generating embeddings
qdrant-client==1.10.1 # For interacting with the Qdrant vector database
psycopg2-binary==2.9.9 # For PostgreSQL database connectivity
sqlalchemy==2.0.31 # For database ORM

# Phase 3
rank-bm25==0.2.2 # For BM25 ranking of retrieved documents
anthropic==0.28.0 # For interacting with Anthropic's API
fastapi==0.111.0 # For building the API
uvicorn[standard]==0.30.1 # For running the FastAPI server

# Phase 4
ragas==0.2.15
datasets==2.20.0
langchain-anthropic==0.3.10
langchain-huggingface==0.1.2