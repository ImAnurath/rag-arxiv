# Phase 1
arxiv==2.1.0 # for data collection
pymupdf==1.24.5 # for PDF parsing — much better than alternatives like pdfminer or PyPDF2
langchain-text-splitters==0.2.2 # for splitting papers into chunks
pydantic==2.7.1 # for data validation in API and evaluation
pydantic-settings==2.3.4 # for managing config via environment variables
python-dotenv==1.0.1 # for loading environment variables from .env file
loguru==0.7.2 # for logging
httpx==0.27.0 # for making HTTP requests to LLM APIs and between API components

# Phase 2
sentence-transformers==3.0.1 # for generating embeddings for retrieval and RAGAS evaluation
qdrant-client==1.10.1 # for vector database to store paper chunk embeddings
psycopg2-binary==2.9.9 # for PostgreSQL client to store paper metadata and provenance info
sqlalchemy==2.0.31 # for ORM to interact with PostgreSQL database

# Phase 3
rank-bm25==0.2.2
anthropic==0.28.0
fastapi==0.111.0
uvicorn[standard]==0.30.1
openai==1.35.0          # also used for Gemini/Groq/Together

# Phase 4
ragas==0.2.15
datasets==2.20.0
langchain-anthropic==0.3.10
langchain-huggingface==0.1.2